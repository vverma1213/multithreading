package org.example.parallelcomputing;
/*
    when we use standard sequential approach then the programming language executes the operation one after another

    multithreaded approach multiple threads but these threads are executed with the time slicing algorithm
    multithreading is used to execute independent tasks without blocking other tasks.

    With Parallel programming we execute different tasks on different processor (or processor cores) simultaneously

    Parallelization is used to speed up given application

    Divide tasks into individual subsets


    if there are multiple processor cores then operating system may execute the threads in parallel

    e.g if machines has 8 cores then 8 threads can be executed 
 */

public class ParallelComputingExample {
}
